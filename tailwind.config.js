/** @type {import('tailwindcss').Config} */
export default {
    theme: {
      extend: {
        fontFamily: {
          syne: ['Syne', 'sans-serif'], // Tambahkan font Syne
        },
      },
    },
    plugins: [],
  };
  